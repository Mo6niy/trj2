pipeline {
    agent any
    // Create parameters for accept from remote trigger
    parameters {
        string(name: 'ID', defaultValue: '')
        
        
    }
    // Execute stages
    stages {
        stage('test stage') {
            steps {
                echo "Hello my parameter ID is: ${params.ID}"
            }
        }
    }
}